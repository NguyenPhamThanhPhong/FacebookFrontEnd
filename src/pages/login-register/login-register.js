import { useContext } from "react"
import { UserContext } from "@data-store/index"



function LoginRegister(props) {

    // const [state,dispatch] = useContext(userContext)

    return (
        <div>
            <h1>login here</h1>
        </div>
    )
}

export default LoginRegister
